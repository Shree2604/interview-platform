{
  "name": "interview-app-server",
  "version": "1.0.0",
  "description": "Backend server for interview registration app with MongoDB storage",
  "main": "server.js",
  "scripts": {
    "start": "set PORT=5000 && node server.js",
    "dev": "set PORT=5000 && nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "interview",
    "registration",
    "mongodb",
    "express",
    "file-upload"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@langchain/langgraph": "^0.1.9",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mammoth": "^1.6.0",
    "mongoose": "^7.5.0",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
